{
  "devDependencies": {
    "pre-commit": "^1.2.2",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/*.js\"",
    "transpile": "tsc"
  },
  "pre-commit": [
    "format",
    "build"
  ],
  "dependencies": {}
}
